name: test_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.6
  - numpy
  - scipy
  - emcee
  - statsmodels
  - xarray
  - pytest
  - pytest-mock
  - coverage
  - pip
  - pip:
      - metacsv
